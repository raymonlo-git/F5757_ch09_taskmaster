---
allowed-tools: Batch(git add:*), Bash(git commit:*), Bash(git diff:*)
argument-hint: [new file added] or "." for all files
description: git commit automation
---

1. Add $ARGUMENTS to the stage
2. Examine the changes in the stage
3. Compose the appropriate git commit messages
4. Perform git commit 
